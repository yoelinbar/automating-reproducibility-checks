# Reproduction Report — Wang (2013), Marriage Networks, Nepotism, and Labor Market Outcomes in China

**Paper:** Wang, Shing-Yi. 2013. "Marriage Networks, Nepotism, and Labor Market Outcomes in China." *American Economic Journal: Applied Economics*, 5(3): 91–112. DOI: 10.1257/app.5.3.91
**Paper IDs:** paper_id=7d4J; claims=[7d4J_single-trace]
**Reproduction type:** Push Button
**Headline verdict (unpatched):** precise
**Verdict if patched:** N/A — no patch needed

## Headline

Push Button reproduction with the author's openICPSR replication archive (DOI 10.3886/E113863V1, manually fetched and placed by the RA) **precisely reproduces** the focal claim. Running `slcc_regressions_male.do` line 7 (Table 2 column 3, SLCC parsimonious specification) on `slcc.dta` returns `post_fILdie` coefficient = -0.06899, robust SE = 0.034073, N = 13,292, t = -2.02, p = 0.043. Rounded to the paper's printed precision: coefficient -0.069 (matches), SE 0.034 (matches), N 13,292 (matches), p < 0.05 (matches the asterisk in the paper). No deviation; the file inventory in the openICPSR archive is exactly the SLCC + CHNS data files plus the do-files described in `readme.pdf`, and no environment edits were required.

## Comparison table

| Claim | Sub-outcome | Paper | Extracted | Reproduced (unpatched) | Bounds (computed) | Sub-verdict |
|---|---|---|---|---|---|---|
| 7d4J_single-trace | Coefficient | -0.069 | -0.069 | -0.06899 → -0.069 | precise = -0.069; approx [-0.0794, -0.0587] | precise |
| 7d4J_single-trace | Sample size | 13,292 | 13,292 | 13,292 | precise = 13,292; approx [11,298, 15,286] | precise |
| 7d4J_single-trace | p-value | "p < 0.05" (`*`) | 0.05 (less-than) | 0.043 | precise ≤ 0.05; approx (0.05, 0.10]; not > 0.10 | precise |
| 7d4J_single-trace | Effect size | -0.069 (regression coefficient) | -0.069 | -0.06899 → -0.069 | precise = -0.069; approx [-0.0794, -0.0587] | precise |
| 7d4J_single-trace | Test statistic | not reported | NaN | t = -2.02 | n/a | non-outcome |

Overall verdict (unpatched): **precise** (every reported sub-outcome is precise).

## What I did

1. **Set-up.** Created `independent_reproductions/7d4J_wang_2013/`, copied `papers/7d4J.pdf`, ran `pdftotext -layout` → `7d4J.txt`. The RA provided the openICPSR replication archive at `manually downloaded data/7d4J/AEJ-App2012-0250-replication_files/`. I copied all archive files (4 SLCC do-files, 4 CHNS do-files, `slcc.dta`, `chns.dta`, `readme.pdf`) into the reproduction directory.

2. **Extraction check.** The single claim's printed source is **Table 2** of the AEJ:Applied published version. The relevant rows of Table 2 column 3 read:
   ```
   PostFILdeath              -0.069*
                             [0.034]
   Observations              13,292
   ```
   Column 3 is the SLCC parsimonious specification matching the claim's `claim_test_spec` ("a cubic in age, education, province-year indicators, individual fixed effects, and a constant term"). Extracted values (coef -0.069, SE 0.034, p < 0.05 from `*`, N 13,292, effect -0.069) match the paper to all reported digits. **No extraction-vs-paper mismatch.**

3. **Data/code source.** openICPSR DOI 10.3886/E113863V1 (project 113863, "AEJ-App2012-0250-replication_files"), fetched manually by the RA. The archive contains:
   - `slcc.dta` (SLCC microdata) and `chns.dta` (CHNS microdata)
   - `slcc_regressions_male.do`, `slcc_regressions_fem.do`, `slcc_summary.do`
   - `chns_regressions_male.do`, `chns_regressions_fem.do`, `chns_regressions_hh.do`, `chns_summary.do`
   - `readme.pdf`, `LICENSE.txt`

4. **Reproduction commands and runtime.** Renamed `slcc_regressions_male.do` → `run_pushbutton_slcc_male.do` (no edits to the script itself). Ran:
   ```
   stata-mp -b do slcc_regressions_male.do
   ```
   in the reproduction directory. Stata 16.0 MP, runtime ~5 seconds. Log saved as `run_pushbutton_slcc_male.log`. **No environment edits to the analysis logic, paths, or variable references.** The `xi:` prefix and `areg ... absorb(id) robust` syntax both run cleanly under Stata 16.

5. **Stochastic-method diagnostics.** N/A — OLS with absorbed individual fixed effects is deterministic given the data.

6. **Classification.** Reproduced output for Table 2 col 3 (line 7 of the do-file):
   ```
   post_fILdi~e |    -.06899    .034073    -2.02   0.043    -.1357798   -.0022002
   ```
   N = 13,292.
   - Coefficient: -0.06899 → rounded to 3 dp = -0.069 = paper → **precise**.
   - SE: 0.034073 → rounded to 3 dp = 0.034 = paper. (SE not in the SCORE rubric criteria list, but recorded for completeness.)
   - N: 13,292 = paper exactly → **precise**.
   - p-value: paper threshold is `< 0.05` (asterisk); reproduced p = 0.043 ≤ 0.05 → **precise**.
   - Effect size (= regression coefficient): same as coefficient → **precise**.
   - Test statistic: not separately reported in paper → **non-outcome**.
   Overall: **precise**.

## Diagnostics — author-bug analysis

Diagnostics: none — unpatched run reproduces the paper's printed Table 2 col 3 values exactly to the paper's reported precision; no bug suspected.

## Files in this folder

- `7d4J.pdf` — copy of the paper's published version of record.
- `7d4J.txt` — `pdftotext -layout` extraction of `7d4J.pdf`.
- `slcc.dta`, `chns.dta` — data files from the openICPSR replication archive.
- `readme.pdf` — author's readme from the replication archive.
- `run_pushbutton_slcc_male.do` — the unpatched do-file used for the headline run (renamed from `slcc_regressions_male.do`; otherwise unchanged).
- `run_pushbutton_slcc_male.log` — Stata log for the headline Push Button run, containing the Table 2 col 3 reproduction.
- `slcc_regressions_fem.do`, `slcc_summary.do`, `chns_regressions_male.do`, `chns_regressions_fem.do`, `chns_regressions_hh.do`, `chns_summary.do` — other do-files from the archive (not run; not relevant to the focal claim).
- `REPRODUCTION_REPORT.md` — this file.
- `7d4J_summary_rows.csv` — one row per claim, summary format.
